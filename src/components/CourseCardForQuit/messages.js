/*
 * CourseCardForQuit Messages
 *
 * This contains all the text for the CourseCardForQuit component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  quit: {
    id: 'app.components.CourseCardForQuit.quit',
    defaultMessage: '退出(QUIT)'
  },
  unactive: {
    id: 'app.components.CourseCardForQuit.unactive',
    defaultMessage: '已锁定(LOCKED)'
  }
});
