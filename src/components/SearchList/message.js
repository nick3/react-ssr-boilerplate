/*
 * SearchList Messages
 *
 * This contains all the text for the SearchList component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  search: {
    id: 'app.containers.SearchList.search',
    defaultMessage: '搜索'
  }
});
