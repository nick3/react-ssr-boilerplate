/*
 * CourseCell Messages
 *
 * This contains all the text for the CourseCell component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  teacher: {
    id: 'app.components.CourseCell.teacher',
    defaultMessage: '教师'
  }
});
