/*
 * SearchBar Messages
 *
 * This contains all the text for the SearchBar component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  cancelTitle: {
    id: 'app.components.SearchBar.cancelTitle',
    defaultMessage: 'Cancel'
  }
});
