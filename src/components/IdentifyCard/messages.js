/*
 * IdentifyCard Messages
 *
 * This contains all the text for the IdentifyCard component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  unregister: {
    id: 'app.components.IdentifyCard.unregister',
    defaultMessage: '未注册'
  }
});
