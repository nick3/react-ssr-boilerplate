/*
 * TopFilterBar Messages
 *
 * This contains all the text for the TopFilterBar component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  filt: {
    id: 'app.containers.TopFilterBar.filt',
    defaultMessage: '过滤'
  }
});
