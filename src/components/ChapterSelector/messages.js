/*
 * ChapterSelector Messages
 *
 * This contains all the text for the ChapterSelector component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  chapter: {
    id: 'app.components.ChapterSelector.chapter',
    defaultMessage: '章节'
  }
});
